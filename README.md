# AntiHub

## 开发与测试

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt -r requirements-dev.txt
python -m pytest -q
```

## 启动

```bash
./dev.sh up
```

更多文档见 `docs/`。
